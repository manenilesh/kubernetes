apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: my-rs-1
  labels:
    app: 
spec:
  template:
    metadata:
      labels:
        app: my-pod-2
    spec:
      containers:
        - name: my-pod-2
          image: nginx
          ports:
            - name: nginx
              containerPort: 80
              protocol: TCP
    replicas: 2
    selector:
      matchLabels:
        app: my-pod-2
