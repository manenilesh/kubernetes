apiVersion: v1
kind: Service
metadata:
  name: my-svc
  namespaces: ns1
spec:
  type: NodePort
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  selector:
    my: my-app
